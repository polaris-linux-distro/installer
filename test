to test, boot an archiso and run this:

# pacman -Sy git lshw wget

and then run:

# git clone https://github.com/polaris-linux-distro/installer.git

enter the dir:

cd installer/src

and finally run it:

python main.py

